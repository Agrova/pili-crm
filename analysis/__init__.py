"""Top-level `analysis/` package — runner scripts for ADR-011/013 pipelines.

Separate from `app.analysis` (schema + models): this package hosts
one-shot / CLI entry points that don't belong inside the module boundary.
"""

from __future__ import annotations
